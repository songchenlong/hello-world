#include<bits/stdc++.h>
using namespace std;

int main()
{
	int t;
	cin >> t;
	while(t--) {
		int a,b;
		cin >> a >> b;
		int c = a - b;
		cout << b; 
		if(c == 0){
			cout << " " << b << endl;
		}	
		else {
			cout << " "<< a/(c+1) << endl;
		}
		
		
	}
	return 0;
 }
